#include "main.h"
/**
 * _islower - function that checks for lowercase character
 *
 * Return: Always 0
 */
int _islower(int c)
{
	int r;

	if (_islower(r) == 1)
	{
		_putchat('1');
	}
	else
	{
		_putchar('0');
	}
	return (0);
}
