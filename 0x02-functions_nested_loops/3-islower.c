#include "main.h"
/**
 * _islower - function that checks for lowercase character
 *
 * Return: Always 0
 */
int _islower(int c)
{
	int c;

	if (_islower(c) == 1)
	{
		_putchar('1');
	}
	else
	{
		_putchar('0');
	}
	return (0);
}
